{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch Moment Service",
        "type": "go",
        "request": "launch",
        "mode": "auto",
        "program": "${workspaceFolder}/main.go",
        "args": ["-conf", "${workspaceFolder}/configs/config.yaml"],
        "env": {}
      }
    ]
}
